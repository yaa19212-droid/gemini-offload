{
  "mcpServers": {
    "gemini-offload": {
      "command": "powershell",
      "args": [
        "-NoProfile",
        "-ExecutionPolicy",
        "Bypass",
        "-File",
        "./scripts/start-gemini-offload.ps1"
      ],
      "env": {
        "GOOGLE_CLOUD_LOCATION": "global"
      },
      "note": "Repo-local stdio MCP wrapper. Set GEMINI_OFFLOAD_REPO and GEMINI_OFFLOAD_VERTEX_CREDENTIALS, optionally GEMINI_OFFLOAD_OUTPUT_DIR, or configure them under mcp_servers.gemini-offload.env."
    }
  }
}
